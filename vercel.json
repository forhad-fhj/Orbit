{
  "buildCommand": "npm run build -w apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --ignore-scripts --package-lock=false",
  "framework": "nextjs",
  "regions": ["iad1"]
}
