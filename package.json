{
  "name": "social-platform-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build --workspace=packages/shared-types --workspace=packages/prisma && npm run build --workspace=apps/api --workspace=apps/web --if-present",
    "build:web": "npm run build -w apps/web",
    "build:api": "npm run db:generate && npm run build -w packages/shared-types -w packages/prisma && npm run build -w apps/api",
    "start": "npm run start --workspaces --if-present",
    "db:push": "npm run db:push -w packages/prisma",
    "db:generate": "npm exec -- prisma generate --schema=./packages/prisma/prisma/schema.prisma",
    "postinstall": "npm run db:generate"
  },
  "dependencies": {
    "prisma": "5.22.0"
  },
  "overrides": {
    "@prisma/client": "5.22.0",
    "prisma": "5.22.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "next": "16.3.1"
  }
}
